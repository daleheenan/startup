# E2E Test Environment Configuration
# Copy this file to .env.test and update with your test values

# Base URL for testing (leave as localhost for local dev)
BASE_URL=http://localhost:3000

# Test credentials
TEST_PASSWORD=testpassword123

# Browser settings
HEADLESS=true
SLOW_MO=0

# Test database (if using separate test DB)
# DATABASE_URL=postgresql://user:pass@localhost:5432/novelforge_test

# CI environment detection (set automatically by CI providers)
# CI=true
